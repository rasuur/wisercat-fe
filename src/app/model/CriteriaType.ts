export enum CriteriaType {
  AMOUNT = "Amount",
  TITLE = "Title",
  DATE = "Date"
}

export enum DateCriteriaType {
  FROM = "From",
  TO = "To"
}

export enum TitleCriteriaType {
  CONTAINS = "Contains",
  NOT_CONTAINS = "Not contains",
  STARTS_WITH= "Starts with",
  ENDS_WITH = "Ends with"
}

export enum YearCriteriaType {
  EQUALS = "Equals",
  GREATER_THAN = "Greater than",
  LESS_THAN = "Less than"
}
